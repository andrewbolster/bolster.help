{
  "name": "bolster.help",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/",
    "test:network": "CHECK_NETWORK=1 node --test tests/",
    "refresh-tools": "node scripts/refresh-tools.mjs"
  }
}
