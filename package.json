{
  "name": "bolster.help",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "WS_NO_BUFFER_UTIL=1 vitest run",
    "test:watch": "WS_NO_BUFFER_UTIL=1 vitest",
    "test:network": "CHECK_NETWORK=1 vitest run --project unit",
    "refresh-tools": "node scripts/refresh-tools.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.2",
    "vitest": "^4.1.11"
  }
}
